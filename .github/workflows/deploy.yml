name: Deploy to Yandex Cloud
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Clean S3 Bucket
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.YANDEX_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.YANDEX_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ru-central1
        run: |
          aws s3 rm s3://${{ secrets.YANDEX_BUCKET_NAME }} --recursive \
            --endpoint-url=https://storage.yandexcloud.net
      - name: Deploy to Yandex S3
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.YANDEX_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.YANDEX_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ru-central1
        run: |
          aws s3 sync ./ s3://${{ secrets.YANDEX_BUCKET_NAME }} \
            --endpoint-url=https://storage.yandexcloud.net \
            --exclude "*" \
            --include "build/*" \
            --include "media/*"
